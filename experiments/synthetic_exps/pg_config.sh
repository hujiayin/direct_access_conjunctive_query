# pg_config.sh
PG_DB_USER=pg_user_name # PostgreSQL username
PG_DB_HOST=pg_host # PostgreSQL host
PG_DB_PORT=pg_port # PostgreSQL port

# Set ITERS as needed to repeat the execution times because some queries may take longer time.
# ITERS=5 # Number of iterations for each query execution